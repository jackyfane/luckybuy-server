const BasicController = require('./BasicController');

/**
 * 商品URL相关控制
 */
class GoodsUrlController extends BasicController {
  constructor(modelName) {
    super(modelName);
  }
}

module.exports = GoodsUrlController;