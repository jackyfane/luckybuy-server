const BasicController = require('./BasicController');

/**
 * 商家控制器
 */
class MerchantController extends BasicController {
  constructor(modelName) {
    super(modelName);
  }
}

module.exports = MerchantController;