const KV = require("./KV")
const CallBack = require("./CallBack")

/** 查询操作：增、删、改、查 */
module.exports = class QueryAction {

  /**
   * 增
   * @param {KV} data 参数
   * @param {CallBack} callback 回调
   */
  add(data, callback) {
  }

  /**
   * 删
   * @param {KV} data 参数
   * @param {CallBack} callback 回调
   */
  del(data, callback) {
  }

  /**
   * 改
   * @param {KV} data 参数
   * @param {CallBack} callback 回调
   */
  upd(data, callback) {
  }

  /**
   * 查
   * @param {KV} data 参数
   * @param {CallBack} callback 回调
   */
  get(data, callback) {
  }

}
